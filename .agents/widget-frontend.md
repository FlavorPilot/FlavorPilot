# Widget frontend addendum

Keep the root and local `AGENTS.md` rules in force. This addendum does not replace an existing agent configuration.

Canonical widget rules: `apps/web/AGENTS.md`.
Architecture and module inventory: `docs/WIDGET_ARCHITECTURE.md`.
Migration and known gaps: `docs/WIDGET_MIGRATION.md`.

Do not call a collection of source files a verified release. Distinguish syntax checks, pure-model tests, fixture screenshots, full dependency-aware builds and live-service tests in every completion report.
